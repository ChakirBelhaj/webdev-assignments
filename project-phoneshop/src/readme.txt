-Installing required packages
To get the server to work we first must install all packages, to do this we run the command "npm install"

-Starting server
To start the server we can run the "npm start" command.

-Api documentation
The API documentation can be found at http://localhost:8080/api-docs

-note
this submission works with our assignment 2 submission. run the server and open index.html for a demo.